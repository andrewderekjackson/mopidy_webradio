============
Installation
============

At the command line::

    $ easy_install mopidy_test

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mopidy_test
    $ pip install mopidy_test
